<Window x:Name="MainScreen" x:Class="TODOList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:local="clr-namespace:TODOList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600" WindowStyle="None" ResizeMode="NoResize">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosing}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"/>
    </WindowChrome.WindowChrome>
    <StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" Background="#3F3F3F">
            <Label DockPanel.Dock="Left" Height="30" Content="TODO List - order your work" Foreground="WhiteSmoke"/>
            <WrapPanel DockPanel.Dock="Left" Width="70" Margin="370,0">
                <Button Command="{Binding Minimize, Mode=TwoWay}" CommandParameter="{Binding ElementName=MainScreen}" WindowChrome.IsHitTestVisibleInChrome="True" Margin="2,0" Style="{DynamicResource Button.Hoverless}" Height="30" Width="30" Background="Transparent">
                    <Image Source="Graphics/minimize.png"/>
                </Button>
                <Button Command="{Binding CloseApp, Mode=TwoWay}" CommandParameter="{Binding ElementName=MainScreen}" WindowChrome.IsHitTestVisibleInChrome="True" Margin="2,0" Style="{DynamicResource Button.Hoverless}" Height="30" Width="30" Background="Transparent">
                    <Image Source="Graphics/close.png"/>
                </Button>
            </WrapPanel>
        </StackPanel>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="420">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Border BorderThickness="1">
                    <Button x:Name="ItemBtn" Command="{Binding Open}" CommandParameter="{Binding Tag, ElementName=ItemBtn}" Tag="Item" Style="{DynamicResource Button.Hoverless}" HorizontalAlignment="Left" Width="40" Height="40" Background="Transparent">
                        <Image Source="Graphics/plus.png" />
                    </Button>
                </Border>
                <ListBox x:Name="ItemsPanel" Height="380" ItemsSource="{Binding Tasks, Mode=OneWay, NotifyOnSourceUpdated=True}" SelectedItem="{Binding Selected}" SelectedIndex="{Binding Index}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Width="40" Height="40" Source="{Binding Priority}"/>
                                <TextBlock VerticalAlignment="Center" Text="{Binding Title, Mode=OneWay}"/>
                                <Grid>
                                    <ProgressBar Width="150" Value="{Binding Percent, Mode=OneWay}" Minimum="0" Maximum="{Binding Count, Mode=OneWay}" Foreground="#FF05EA2F"/>
                                    <TextBlock Text="{Binding PercentString, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="Graphics/confirm.png"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
            <StackPanel Grid.Column="1" Background="DarkGray">
                <Grid>
                    <Label Content="Title:" Width="35" HorizontalAlignment="Left" Margin="10,10"/>
                    <TextBlock Text="{Binding Selected.Title}" Width="180" HorizontalAlignment="Left" FontSize="18" TextAlignment="Center" Margin="60,10"/>
                </Grid>
                <Grid Margin="0,20" Height="80">
                    <Label Content="Added in:" Width="60" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <Label Content="Deadline:" Width="60" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,25"/>
                    <TextBlock Text="{Binding Selected.AddDateString}" Width="100" Height="30" FontSize="18" Margin="70,0" TextAlignment="Center" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <TextBlock Text="{Binding Selected.DeadlineString}" Width="100" Height="30" FontSize="18" Margin="70,25" TextAlignment="Center" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <Label Content="Completed:" FontSize="8" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,55,0,0"/>
                    <TextBlock Text="{Binding Selected.CompletedString}" Width="50" Height="30" FontSize="10" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="50,55,0,0"/>
                </Grid>
                <Grid Margin="0,0">
                    <ProgressBar Width="200" Height="25" Value="{Binding Selected.Percent,Mode=OneWay}" Minimum="0" Maximum="{Binding Selected.Count}" Foreground="#FF05EA2F" />
                    <TextBlock Text="{Binding Selected.PercentString}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <ListView x:Name="SubItemsPanel" Height="150" Width="230" Margin="0,20" ItemsSource="{Binding Selected.SubItems}">
                    <ListView.ItemTemplate>
                        <HierarchicalDataTemplate>
                            <WrapPanel>
                                <Image Width="40" Height="40" Margin="5,0" Source=""/>
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Stretch" Width="100" Text="{Binding Title}"/>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="Graphics/confirm.png"/>
                                </Button>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="graphics/delete.png"/>
                                </Button>
                            </WrapPanel>
                        </HierarchicalDataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <Button x:Name="SubItemBtn" Grid.Column="1" Background="Transparent" Tag="SubItem" Style="{DynamicResource Button.Hoverless}" Command="{Binding Open}" CommandParameter="{Binding Tag, ElementName=SubItemBtn}" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <Image Source="Graphics/plus.png"/>
            </Button>
        </Grid>
    </StackPanel>
</Window>
