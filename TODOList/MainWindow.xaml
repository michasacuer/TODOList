<Window x:Class="TODOList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gif="http://wpfanimatedgif.codeplex.com"
        xmlns:local="clr-namespace:TODOList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600" WindowStyle="None" ResizeMode="NoResize">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"/>
    </WindowChrome.WindowChrome>
    <StackPanel>
        <StackPanel Orientation="Horizontal" Height="30" Background="#3F3F3F">
            <Label DockPanel.Dock="Left" Height="30" Content="TODO List - order your work" Foreground="WhiteSmoke"/>
            <WrapPanel DockPanel.Dock="Left" Width="70" Margin="370,0">
                <Button x:Name="MinimizeWindowButton" WindowChrome.IsHitTestVisibleInChrome="True" Margin="2,0" Style="{DynamicResource Button.Hoverless}" Height="30" Width="30" Background="Transparent">
                    <Image Source="Images/minimize.png"/>
                </Button>
                <Button x:Name="CloseWindowButton" WindowChrome.IsHitTestVisibleInChrome="True" Margin="2,0" Style="{DynamicResource Button.Hoverless}" Height="30" Width="30" Background="Transparent">
                    <Image Source="Images/close.png"/>
                </Button>
            </WrapPanel>
        </StackPanel>
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="420">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Border BorderThickness="1">
                    <Button Style="{DynamicResource Button.Hoverless}" HorizontalAlignment="Left" Width="40" Height="40" Background="Transparent">
                        <Image Source="Images/plus.png" />
                    </Button>
                </Border>
                <ListView x:Name="ItemsPanel" Height="380" ItemsSource="{Binding}">
                    <ListView.ItemTemplate>
                        <HierarchicalDataTemplate>
                            <WrapPanel Background="Gray">
                                <Image Width="40" Height="40" Source="{Binding Priority,Converter={x:Static local:HeaderToImageConverter.Instance}}"/>
                                <TextBlock VerticalAlignment="Center" Text="{Binding}"/>
                                <Grid>
                                    <ProgressBar Width="150" Value="{Binding}" Minimum="0" Maximum="{Binding}" Foreground="#FF05EA2F"/>
                                    <TextBlock Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="Images/confirm.png"/>
                                </Button>
                            </WrapPanel>
                        </HierarchicalDataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <StackPanel Grid.Column="1" Background="DarkGray" >
                <Grid>
                    <Label Content="Title:" Width="35" HorizontalAlignment="Left" Margin="10,10"/>
                    <TextBlock Text="Test" Width="180" HorizontalAlignment="Left" FontSize="18" TextAlignment="Center" Margin="60,10"/>
                </Grid>
                <Grid Margin="0,20" Height="80">
                    <Label Content="Added in:" Width="60" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <Label Content="Deadline:" Width="60" Height="30" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,25"/>
                    <TextBlock Text="date" Width="100" Height="30" FontSize="18" Margin="70,0" TextAlignment="Center" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <TextBlock Text="date" Width="100" Height="30" FontSize="18" Margin="70,25" TextAlignment="Center" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                </Grid>
                <Grid Margin="0,0">
                    <ProgressBar Width="200" Height="25" Value="1" Minimum="0" Maximum="1" Foreground="#FF05EA2F" />
                    <TextBlock Text="100%" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <ListView x:Name="SubItemsPanel" ItemsSource="{Binding}" Height="150" Width="230" Margin="0,20">
                    <ListView.ItemTemplate>
                        <HierarchicalDataTemplate>
                            <WrapPanel>
                                <TextBlock VerticalAlignment="Center" Text="{Binding Title,Mode=OneWay}"/>
                                <Grid>
                                    <ProgressBar Width="150" Value="{Binding Percent,Mode=OneWay}" Minimum="0" Maximum="{Binding Count,Mode=OneWay}" Foreground="#FF05EA2F"/>
                                    <TextBlock Text="{Binding Percent,Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="Images/confirm.png"/>
                                </Button>
                                <Button HorizontalAlignment="Right" Width="40" Height="40" Background="Transparent" Style="{DynamicResource Button.Hoverless}">
                                    <Image Source="Images/delete.png"/>
                                </Button>
                            </WrapPanel>
                        </HierarchicalDataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Background="Transparent" Style="{DynamicResource Button.Hoverless}" Width="40" Height="40" Margin="0,-5">
                    <Image Source="Images/plus.png"/>
                </Button>
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
